[project]
name = "jaffle-shop"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "jafgen==0.4.12",
    "pre-commit==3.7.0",
    "requests==2.31.0",
]

[tool.setuptools]
packages = []
